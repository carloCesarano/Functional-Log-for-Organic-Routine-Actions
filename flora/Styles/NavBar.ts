import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    navbarP: {
        width: '100%',
        justifyContent: 'space-between',
        flexDirection: 'row',
    },
    // <NavBar> -> <View> -> <Icona> -> <Image>
    icona: {
        width: 50,
        height: 50,
    },
})