import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    button: {
        width: '100%',
        height: 44,
        backgroundColor: '#FFF',
        borderRadius: 8,
        paddingHorizontal: 12,
        borderWidth: 0,
    },

    testo: {
        fontSize: 16,
        color: 'BLACK',
    }
})