import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    toast: {
        position: 'absolute',
        top: 80,
        zIndex: 1000,
        elevation: 1000,
    },

    contenuto: {
        paddingHorizontal: 15,
    },

    titolo: {
        fontSize: 18,
        fontWeight: '400',
    },

    messaggio: {
        fontSize: 14,
    },
})